module.exports = {
    content: ['./src/**/*.{js,jsx,ts,tsx}'],
    darkMode: 'class', // Enables dark mode based on the `class` attribute.
    theme: {
      extend: {},
    },
    plugins: [],
  };
  